<Window x:Class="WPF.SalesManagementSystem.ProductManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.SalesManagementSystem"
        mc:Ignorable="d"
        Title="ProductManagementWindow" Height="450" Width="800">
    <Grid>
        <ListView d:ItemsSource="{d:SampleData ItemCount=5}" Margin="34,68,277,217">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID"
                                    DisplayMemberBinding="{Binding ProductId}"/>
                    <GridViewColumn Header="Name"
                                    DisplayMemberBinding="{Binding ProductName}"/>
                    <GridViewColumn Header="Category"
                                    DisplayMemberBinding="{Binding Category}"/>
                    <GridViewColumn Header="QuantityPerUnit"
                                    DisplayMemberBinding="{Binding QuantityPerUnit}"/>
                    <GridViewColumn Header="Price"
                                    DisplayMemberBinding="{Binding UnitPrice}"/>
                    <GridViewColumn Header="Quantity"
                                    DisplayMemberBinding="{Binding UnitsInStock}"/>
                </GridView>
            </ListView.View>
        </ListView>
        
        <Button x:Name="btnCreate" Content="Create" HorizontalAlignment="Left" Margin="34,249,0,0" VerticalAlignment="Top"/>
        <Button x:Name="btnUpdate" Content="Update" HorizontalAlignment="Left" Margin="106,249,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.286,-0.163"/>
        <Button x:Name="btnDelete" Content="Delete" HorizontalAlignment="Left" Margin="187,249,0,0" VerticalAlignment="Top"/>

    </Grid>
</Window>
